//these enable us to write variables with strings in the same sentence (interpolation)
//they act like the print(f'') in python 

//to use template literals we have to use the backticks `` followed by a ${variableNameHere}
let fName = 'James';
let lName = 'Gunn';
let fullName = `${fName} ${lName} is a good director NGL`;

console.log(fullName);











